version: "3"
agent:
    authtoken: YOUR_NGROK_AUTH_TOKEN
    log: stdout
tunnels:
    backend:
        proto: http
        addr: backend:5000
    frontend:
        proto: http
        addr: frontend:3000
